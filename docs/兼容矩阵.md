# 兼容矩阵（v1/v2 × 交互/headless）

发版门禁（AGENTS.md D4）：四象限逐版实测。

| 象限 | v1（legacy，`KIMI_CODE_LEGACY_FLAG=1`） | v2（默认引擎） |
|---|---|---|
| headless（`kimi -p`） | ✅ kimi 0.34.0，2026-08-13 实测：hook 正常触发，payload 落盘；字段 = 公共 6 字段（无 `client_type`）；`cwd` 正斜杠 | ✅ kimi 0.34.0，2026-08-13 实测：hook 正常触发；字段 = 公共 6 字段 + `client_type`；`cwd` 反斜杠 |
| 交互 TUI（`kimi`） | ✅ kimi 0.36.0，2026-08-14 实测：SessionStart + PreToolUse(Bash) 均触发并经事件管道入库（audit id 609/611） | ✅ kimi 0.36.0，2026-08-14 实测：SessionStart + PreToolUse(Bash) 均触发并经事件管道入库（audit id 589/604） |
| web（`kimi web`） | ⬜ 未实测（入口已标 deprecated，补测优先级低） | ⬜ 未实测（同上） |

证据：headless 两格 = `fixtures/`（14 条真实 payload，双引擎 × 6 工具）；交互两格 = M3 事件管道 audit.db 实测记录（2026-08-14）。

实测补充（0.36.0 交互 TUI）：

- TUI 懒建会话：窗口启动后不发任何 hook，**首发消息才建会话并触发 SessionStart**。
- `SessionStart` 与 `PreToolUse` 在交互模式下均走 `[[hooks]]` 注入块，双引擎行为一致。
- 注：0.34.0 起 v2 已是默认引擎，`KIMI_CODE_LEGACY_FLAG=1` 回退 v1
  （源码佐证：`参考/kimi-code-main/apps/kimi-code/src/cli/experimental-v2.ts`）。
