# 兼容矩阵（v1/v2 × 交互/headless）

发版门禁（AGENTS.md D4）：四象限逐版实测。当前实测版本：**kimi 0.34.0**，2026-08-13。

| 象限 | v1（legacy，`KIMI_CODE_LEGACY_FLAG=1`） | v2（0.34.0 默认） |
|---|---|---|
| headless（`kimi -p`） | ✅ 实测：hook 正常触发，payload 落盘；字段 = 公共 6 字段（无 `client_type`）；`cwd` 正斜杠 | ✅ 实测：hook 正常触发；字段 = 公共 6 字段 + `client_type`；`cwd` 反斜杠 |
| 交互 TUI（`kimi`） | ⬜ 未测（补采步骤见 `fixtures/README.md`） | ⬜ 未测 |
| web（`kimi web`） | ⬜ 未测 | ⬜ 未测 |

证据：`fixtures/`（14 条真实 payload，双引擎 × 6 工具）。

注：0.34.0 中 v2 已是 headless 默认引擎，`KIMI_CODE_LEGACY_FLAG=1` 回退 v1
（源码佐证：`kimi-code-main/apps/kimi-code/src/cli/experimental-v2.ts`）。
